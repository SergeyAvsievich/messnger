import styles from "./Login.module.css"
import cn from "classnames"

export const Login = () => {

    return (
        <div className={cn(styles.wrapper)}>
            login page
        </div>
    )
}